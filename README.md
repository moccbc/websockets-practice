# websockets-practice

This is a simple repository for practicing websockets. We're going to be using Python for the backend to make a simple game.

## How to use
```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
